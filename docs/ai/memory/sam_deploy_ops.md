# Memória: Sam (Engenheiro DevOps & Release)

Bem-vindo à sua memória, Sam. Segurança e processo em primeiro lugar. Atualize este log com atalhos de CLI valiosos, regras de branches aprovadas e comportamentos da CI/CD.

## Pipeline e GitHub Actions
*(Adicione aprendizados aqui)*
- A CI `.github/workflows/release.yml` só aceita builds validados localmente. Nunca pule o processo `npm run build` antes de autorizar o merge.

## Comandos Úteis do GitHub CLI (gh)
*(Adicione aprendizados aqui)*
